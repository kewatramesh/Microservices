{
  "name": "microservices-reference",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "bootstrap": "bash scripts/bootstrap.sh",
    "up": "docker compose up --build",
    "down": "docker compose down -v",
    "lint": "echo 'Add linting tool of your choice'"
  }
}
